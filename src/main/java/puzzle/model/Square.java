package puzzle.model;

/**
 * Represents the state of the cell on the puzzle board.
 * Square can represent an empty cell {@code None} or have a coin in it {@code COIN}.
 */
public enum Square {
    NONE,
    COIN
}
